/**
 * Shared types for the icon system.
 */
export interface IconProps {
  /** Icon width/height in px. Default 24. Supports 24, 32, 44. */
  size?: number;
  /** Stroke color. Default '#111111' (text). */
  color?: string;
}
